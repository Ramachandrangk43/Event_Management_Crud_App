# Alternative requirements for development (Windows-friendly)
Django>=4.2.0,<5.0.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0
django-filter>=23.0.0
python-decouple>=3.8

# Use this instead of psycopg2-binary if you have issues
# psycopg2>=2.9.0

# For SQLite development (no PostgreSQL required)
# Comment out the psycopg2 line above and Django will use SQLite by default